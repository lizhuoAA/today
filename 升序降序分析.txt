升序降序分析：
    1.首先要知道页面中展示的数据原本都应该是直接通过ajax请求从后台取来的数据，应该是一个json数组对象
        但是因为我们这是静态的死页面，所有数据都是写死进去的，无法直接做升序或者降序。
    2.我们要想做效果，需要把其中的所有的数据都取出来，放入一个数组中，
        一个商品中包含的(价格，图片路径，名称,唯一标识串码商品编号)就是一个对象。再把这些商品对象
        放入一个数组中去。
    3.对做好的这个json数组做DOM遍历 根据页面中写好的模板去动态的生成各种标签，放到该放置的位置去。